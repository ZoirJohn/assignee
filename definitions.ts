export type TMessage = {
        attachments: null | JSON
        content: string
        conversation_id: string
        created_at: string
        id: string
        is_edited: boolean
        receiver_id: string
        sender_id: string
        status: string
        updated_at: string
}
export type TStudent = {
        id: string
        full_name: string
}
